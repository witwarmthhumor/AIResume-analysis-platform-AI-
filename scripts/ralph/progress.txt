# Ralph Progress Log

Feature: Agent 工具扩展（4 → 11 个）
Branch: ralph/agent-tool-expansion
Started: 2026-09-16

## Codebase Patterns

- 工具层在 `backend/app/services/agent/tools.py`，`make_tools(db, user_id, anonymous_id, ctx)` 是**每请求闭包工厂**，工具实例绑定本次请求的 db 会话与归属者。
- **`kb_search` 必须始终是 `make_tools` 返回列表的第一个元素** —— `tests/test_agent.py` 与 `tests/test_agent_tools.py` 依赖顺序。
- 归属过滤统一走 `_owner_filter(model, user_id, anonymous_id)`：登录按 `model.user_id == user_id`，匿名按 `model.user_id.is_(None) & (model.anonymous_id == x)`；识别不到身份要**明确拒绝**而不是返回全部数据。
- 工具内部**必须吞掉异常**返回自然语言兜底 —— 工具无法返回 HTTP 状态码，抛异常会让整轮 Agent 崩。
- 工具返回值是给 LLM 读的**自然语言**，必须截断（单块内容 300 字上限的先例）。
- JSONB 列写入 Python `None` 会落成 JSON `null` **字面量**而非 SQL NULL，`is_not(None)` 过滤不掉，必须再判 `isinstance(value, dict)`。
- 限额/记账复用 `usage_service.count_today_usage_by_owner(db, action_type, user_id, anonymous_id)` 与 `usage_service.write_usage(...)`，不要新写 SQL。
- 测试要在 `backend/` 目录下跑（`.env` 相对当前目录读）；新测试用专属标记只清自己造的数据，不要 `DELETE FROM` 全表。
- ⚠️ 跑全量 pytest 会清空 `kb_chunks` / `kb_documents`（老 `tests/test_rag.py` fixture 行为），跑完需 `python -m scripts.seed_kb_preset` 恢复语料。
- 前端零图表库、零 UI 框架：可视化一律手写 SVG/CSS；组件内映射表用常量 + 回落原始值（参考 `HistoryView.vue` 的 `actionMap`）。
- **准入检查只有 `ruff check .`**：`ruff format --check` 对 `tools.py` 等既有文件本来就报警（HEAD 即如此），是前格式化时代留下的。**别顺手整文件格式化**（会造出大量与 story 无关的 diff），按邻近代码风格写即可。
- 工具在 `services/` 层**不得 import `app/api/`**（项目是 Router → Service → Model 单向依赖）。需要复用接口层查询时，先把它下沉成 service 函数，接口与工具都调那一份，不要复制 SQL。
- `created_at` 是 `server_default=func.now()`（事务开始时间），**同一事务插入的多行时间戳可能完全相同**——所有「取最近 N 条」查询必须带 `id desc` 兜底排序；测试里要控制先后就**显式传 `created_at=`**（`InterviewSession(created_at=...)` 会覆盖 server_default）。
- ⚠️ 开跑 pytest 前先确认 Docker Desktop 起来了：本项目容器是**已存在但 stopped**，用 `docker start ai-interview-db ai-interview-redis ai-interview-ollama`；`docker compose up -d` 会报 name conflict。DB 没起时 pytest 会**挂住不报错**（连接超时很长），别误判成死循环。
- 知识库可见性判据是 `kb_service._visible_clause`（`scope=public` 全站可见 / `private` 仅归属者），工具与接口都复用它；但测试里造的 `scope=public` 文档 `user_id` 与 `anonymous_id` **都是空**（与预置语料同形），按归属者清理清不掉 —— 测试数据要带**标题前缀**，清理时用 `title LIKE '前缀%'` 兜底（`tests/test_agent_tools.py` 的 `_KB_PREFIX`）。
- 开发库长期有 7 篇预置语料，凡「空库 / 精确条数」类断言都会被它干扰：要么只断言与总数无关的片段（如「仅显示前 20 篇」），要么 monkeypatch 掉查询函数隔离验证。
- **纯静态工具（如 `platform_help`）用同一套「别名元组 → 文案」路由**：对 topic 与别名都做小写化再判包含，**别名顺序即优先级**（具体主题排前，防泛词抢匹配），空 topic 或全不命中回落总览；这类工具不查库，`db` 传 `None` 也能跑通。
- 工具 `description` 要三段式（做什么 / 什么时候用 / 什么时候**不**用），易撞的工具互相点名排他（如 `kb_search` ↔ `platform_help`、`resume_lookup` ↔ `analysis_read`）。
- 描述的可校验写法：统一用**中文冒号小标题**「什么时候用：」「什么时候不用：」当锚点，测试断言 `len(description) > 50` 且两个锚点都在；再加一条「参数化清单 == `make_tools` 实际返回」的测试，新增工具漏写描述会直接红。
- 改描述属于**纯 docstring 改动**，不影响任何工具行为：断言描述不需要数据库，`make_tools(None, None, None, ToolContext())` 取 `.description` 即可（与 US-005 证明静态工具不查库同一手法）。
- ⚠️ ruff 实际启用的是**扩展规则集**（含 `RUF100` / `BLE001` / `B008` / `SIM` / `S110`），但 **`E402` 未启用**：照 `scripts/eval_rag.py` 写 `sys.path.insert` 后再 import 时**不要加 `# noqa: E402`**（会被 RUF100 判为多余指令）。
- ⚠️ **`BLE001`（盲吞异常）的免死金牌是"带堆栈地记日志"**：`logger.exception(...)` 或 `logger.warning(..., exc_info=True)` 的处理器**不报**；纯 `return`/赋值/`logger.warning("x")`（无 `exc_info`）才报——`app/main.py:89`、`app/db/session.py:38` 属于后者所以才要 `# noqa: BLE001`。tools.py 里最常见的 `except Exception:` + `logger.exception(...)` + `return 文案` 写法**千万别加 noqa**（反被 RUF100 判为多余）。判据永远是直接跑 `ruff check <文件>`，别照抄别处。
- 工具内部要调模型的场景统一走 `_run_tool_llm(db, user_id, anonymous_id, call)`：返回 `(结果, 兜底话术)` 两者只有一个非空；**超限/`limit=0` 时连 `call` 都不执行**（这是省钱的关闸）；异常不在这一层吞（"JD 太短""回答为空""服务欠费"话术各不相同，由工具自己 try/except）；**成功才记账**（写 `agent_tool_llm` 并 commit）；无归属者不写账。新工具要调模型必须走它，否则限流与成本统计会漏。
- 新增 `action_type` 时记得同步 `tools.py` 的 `_ACTION_LABELS`，否则 `usage_stats` 会把英文枚举直接甩给用户。
- 写可测脚本（CLI 评测类）的套路：把「一次 LLM 调用」抽成注入的 `ask: (question) -> BaseMessage`，`run(cases, ask)` 只做纯统计；测试里用**替身模型**（只需实现 `bind` / `bind_tools` / `invoke` 三个方法返回 `AIMessage(tool_calls=[...])`）即可离线覆盖。测 `main()` 时 monkeypatch 三件套：`load_cases` / `REPORT_PATH`(tmp_path) / `build_chat_llm`。
- 路由评测只需**工具 schema**，不要执行工具：`make_tools(None, None, None, ToolContext())` 拿描述 → `build_chat_llm().bind(temperature=0).bind_tools(tools)` → 读 `msg.tool_calls[0]["name"]`；模型直接文字作答时用 `NO_TOOL` 占位计入未命中。
- ⚠️ 每新增一个工具，`data/agent_eval/routing.json` 必须同步补 3 条用例：`tests/test_eval_agent_routing.py::test_cases_cover_every_tool` 断言「用例数 ≥ 工具数 × 3」，只加工具不加用例全量 pytest 会直接红。
- 提示词版本是**每套提示词一个常量**、互不相干：`prompts.PROMPT_VERSION`（简历分析，入 analyses 表）、`interview_prompts.INTERVIEW_PROMPT_VERSION`、`prompts.JOB_MATCH_PROMPT_VERSION`。**绝不要为了"新增提示词"去递增 `PROMPT_VERSION`** —— 那会让库里所有旧分析报告按「版本过期」失效。
- 工具内调 LLM 的工具（`job_match` / `question_gen` / `answer_review`）实现套路：`_run_tool_llm(db, user_id, anonymous_id, lambda: chat_json(SYSTEM_PROMPT, build_xxx_prompt(...), settings, XxxReport.model_validate))`，**整个调用包一层 try/except 兜话术**（`_run_tool_llm` 不吞异常），AI 输出契约放 `app/schemas/agent.py`，提示词放 `app/services/prompts.py`。
- 测这类工具只需 monkeypatch `app.services.agent.tools.chat_json` 做替身（记录 `system`/`user`/`validator` 三个入参）：成功路径断言渲染文本 + 记账，短路路径（空参数/无数据/超限）断言 `seen == []` 证明一次模型都没调。
- ⚠️ **同一文件上的多个 Edit 不要并行发**：实测在同一条消息里对同一文件发两个 Edit，其中一个会静默丢失（工具回报成功但文件没变），直到 ruff/pytest 报 F821 / 断言失败才发现。改同一文件时**逐个串行改**，改完 grep 关键字确认落地。
- ⚠️ pydantic 的 `Model.model_validate` 每次取出来都是**新对象**，`is` 比较会失败（`assert X is Y` 报 "model_validate is model_validate"）；要断言"用的是哪个校验器"就**调用它**再 `isinstance(结果, 模型)`。
- 需要"先检索再喂给模型"的工具，别去 invoke 另一个工具对象：把检索链路抽成模块级公共入口（本项目是 `_kb_retrieve(db, query, user_id, anonymous_id, top_k) -> (命中块, 兜底话术)`），返回话术而非抛异常，让不同调用方自己决定处置（kb_search 要区分向量模型挂了/检索出错，question_gen 一律当"未收录"退回模型出题）。
- 混合检索（hybrid）路径**不套 `kb_min_similarity`**（设计如此，为救回纯词法命中的术语查询），所以"检索未命中"在真机上几乎不触发——连明显无关的 topic 也会返回低分材料。"未命中"分支只能靠 mock 空结果覆盖，别指望真机复现；输出里标注来源时要意识到这一点。
- ⚠️ **`backend/app/services/prompts.py` 是全仓库唯一的 CRLF 文件**（其余如 `tools.py` / 测试都是 LF）：Edit 的**多行 old_string 匹配不到**（行尾是 `\r\n`），只能用**单行锚点**替换/插入；插入多行时新行按 `\r\n` 写，别把文件搞成混用行尾。改前先 `python -c "print(open(f,'rb').read().count(b'\r\n'))"` 确认。
- 新增/改一个工具的**六处同步点**（漏一处就红）：`make_tools` 返回列表（`kb_search` 仍须下标 0）→ 模块 docstring 工具清单 → `tests/test_agent_tools.py` 的 `_ALL_TOOLS` 与 `test_make_tools_exposes_*_tools` → 易撞组合互指（双向点名 + 测试参数对）→ `data/agent_eval/routing.json` 补 3 条用例 → `docs/Agent工具设计.md` §二表格与 §四易撞表。
- 跨工具复用的评分口径直接遍历 `_SCORE_LABELS`（技术深度/表达结构/项目真实性/整体），只点评单项时 `if key != "overall"` 过滤即可——不要另起一份中文映射，会和模拟面试报告漂移。
- 工具内调 LLM 的三件套（`job_match` / `question_gen` / `answer_review`）里有两类「不需要身份」：`question_gen`（只查公共语料）与 `answer_review`（题目和回答都是用户当场贴的，不查库）——它们没有 `_owner_filter`，但仍走 `_run_tool_llm` 限额与记账。
- 测「入参截断」时填充字符要**避开提示词模板里已有的字**：「答」会撞上 `候选人的回答：` 让计数多 1（`2001 != 2000`），换模板里不存在的字（如「好」）或直接照 job_match 用例用 `x`。

## Log

## 2026-09-16 10:40 - US-001
- What was implemented: 把分析报告查询从 API 层下沉到 service 层。`analysis_service.py` 新增公开函数 `latest_valid_analysis(db, resume_id)`（`valid_json is True` + `prompt_version == PROMPT_VERSION` + `created_at desc, id desc` 取首条），`api/analyses.py` 删除私有 `_latest_valid_analysis`，两处调用点（POST analyze 的缓存命中、GET analysis）改为调用 service 函数。
- Files changed:
  - `backend/app/services/analysis_service.py`（新增 `latest_valid_analysis`，模块 docstring 补「+ 报告查询」）
  - `backend/app/api/analyses.py`（删私有函数、改 import 与两个调用点）
  - `AGENTS.md`（环境现状补一句：容器已存在要用 `docker start`，compose up 会报 name conflict）
- Quality checks: `ruff check .` all passed；`pytest -q` 143 passed；`pytest tests/test_analyses.py` 9 passed。全量跑完 kb 表被清空，已 `python -m scripts.seed_kb_preset` 恢复 7 条预置语料。
- **Learnings for future iterations:**
  - Docker Desktop 不随开机自启，pytest 前必须先 `docker start` 三个容器。DB 连不上时 pytest **静默挂住**（不是报错），第一次跑等了 5 分钟才发现 —— 以后开跑前先 `docker ps` 确认。
  - 全量 pytest 一定清空 kb 表（已在 Codebase Patterns 记录），跑完立刻 seed 恢复，否则后续 US-004 `kb_list` 的单测会拿到空库。
  - `api/analyses.py` 里 `select` / `Analysis` / `Session` / `PROMPT_VERSION` 仍被 `list_resume_analyses`、`_to_out`、`_get_owned_resume` 用到，删函数后不要顺手删这些 import。
  - `latest_valid_analysis` 只服务「可复用」语义；v3.5 的版本对比接口是**另写查询、故意不过滤 prompt_version** 的，不要为了复用去放宽这个函数的条件。
  - 工作区里 `frontend/vite.config.ts` 有一处用户未提交的本地改动（后端端口 8000→8001），与本次 story 无关，**未纳入本次提交**。
---

## 2026-09-16 11:30 - US-002
- What was implemented: 新增第 5 个工具 `analysis_read(resume_hint)`——读当前用户某份简历的 AI 分析**结论**。身份闭包捕获 → 查本人未软删简历（`created_at desc, id desc`）→ hint 为空取最近一份、非空按文件名不区分大小写包含匹配、不匹配则列出实际文件名 → 复用 `analysis_service.latest_valid_analysis` 取有效报告（valid_json=True + 当前 PROMPT_VERSION）→ 渲染目标岗位/岗位匹配/优势/短板/关键词缺口/改进建议/预测面试题，总长截到 800 字符、预测题最多 5 条。
- Files changed:
  - `backend/app/services/agent/tools.py`（新增 `_as_items` 拍平助手、`_TOOL_REPORT_CHARS/_TOOL_REPORT_ITEMS/_TOOL_REPORT_QUESTIONS` 常量、`analysis_read` 工具；`make_tools` 返回列表尾部追加，**kb_search 仍在下标 0**；模块 docstring 工具清单补第 5 项）
  - `backend/tests/test_agent_tools.py`（清表范围加 `analyses` 表；工具清单断言改 5 个；无身份用例补 analysis_read；新增 10 项 analysis_read 测试）
  - `docs/Agent工具设计.md`（补一条硬约束：工具在 services 层不得 import app/api）
  - `scripts/ralph/prd.json`（US-002 passes=true）
- Quality checks: `ruff check .` all passed；`pytest -q` 153 passed（US-001 时 143，本次 +10）；`ruff format --check` 对本文件**仍然报警但属既有问题**（见下）。全量跑完 kb 表被清空，已 `python -m scripts.seed_kb_preset` 恢复 7 条预置语料。
- **Learnings for future iterations:**
  - **`ruff format` 不是这个仓库的准入门槛**：`app/services/agent/tools.py` 在 HEAD 就已被 ruff format 判为「would be reformatted」（至少 6 处，如 `uploaded = ... if ... else ...` 超 88 列、`db.scalars(...).all()` 括号写法）。项目实际检查的是 `ruff check .`（真绿）。**别为了 format 干净去整文件格式化**，会造出一堆与 story 无关的 diff；跟着邻近代码风格写即可。
  - 复用 API 层逻辑前先看它在哪一层：`latest_valid_analysis` 已在 US-001 下沉到 `analysis_service`，工具直接 import 即可。若下个 story 发现要用的函数还留在 `app/api/`，**先下沉再调用**，不要在工具里复制 SQL（tools 层 import app/api 会破坏 Router → Service → Model 单向依赖）。
  - 分析报告的权威判据是 `analysis.service.latest_valid_analysis`（valid_json=True + prompt_version==PROMPT_VERSION）；**不要**自己写 `prompt_version` 过滤，也不要放宽它（v3.5 版本对比接口是故意不过滤版本的另写查询）。
  - `result_json` 是 JSONB，`is None` 判不出来（progress 顶部已记过），渲染前必须 `isinstance(report, dict)` 兜底；本次同时防了「valid_json=false 但 result_json 有内容」的半成品被读出来。
  - 测试里 `_add_analysis` 必须把 `prompt_version` 写成 `PROMPT_VERSION` 常量而不是字面量 "1"——提示词改版递增后字面量会静默让测试全部走「没有报告」分支。
  - `created_at` 用的是 `server_default=func.now()`（= 事务开始时间），同一事务插入的多行时间戳可能完全相同，所以「取最近一份」一定要带 `id desc` 兜底（本次已带）。
  - 工作区里 `frontend/vite.config.ts` 的用户本地改动（后端端口 8000→8001）仍在，与本 story 无关，**未纳入本次提交**。
---

## 2026-09-16 12:10 - US-003
- What was implemented: 新增第 6 个工具 `score_trend(limit=5)`——查本人已结束场次的分数趋势。查 `status == "finished"` 的最近 N 场（倒序取后 `reverse()` 成正序）→ 用 `isinstance(final_report_json, dict)` 排掉 SQL NULL 与 JSON null 字面量 → 逐场渲染技术深度/表达结构/项目真实性/整体表现，每维度带相对上一场的 ↑↓→ 箭头，场次标记用 基准场/上升/下降/持平（缺分数时为「缺对比数据」）→ 末尾给首末整体分差总结。limit 钳到 1~10、非正数回落 5；0 场 / 1 场 / 他人数据各有明确话术。
- Files changed:
  - `backend/app/services/agent/tools.py`（新增 `_score_of` / `_trend_arrow` / `_trend_mark` / `_trend_overall` / `_mean_score` 五个模块级助手、`_TOOL_TREND_LIMIT = 10` 常量、`score_trend` 工具；`make_tools` 返回列表插在 `interview_history` 之后，**kb_search 仍在下标 0**；模块 docstring 工具清单补第 6 项）
  - `backend/tests/test_agent_tools.py`（工具清单断言改 6 个；无身份用例补 score_trend；新增 8 项 score_trend 测试，含 JSON `'null'::jsonb` 字面量用例与 limit 钳制三档）
  - `docs/Agent工具设计.md`（「易撞组合」表补 `interview_history` vs `score_trend` 的划线：单场 vs 跨场次）
  - `scripts/ralph/prd.json`（US-003 passes=true）
- Quality checks: `ruff check .` all passed；`pytest -q` 161 passed（US-002 时 153，本次 +8）；`pytest tests/test_agent_tools.py` 31 passed。全量跑完 kb 表被清空，已 `python -m scripts.seed_kb_preset` 恢复 7 条预置语料。
- **Learnings for future iterations:**
  - **`ruff check` 会报 `ISC004`**：列表里做多行隐式字符串拼接必须整体加括号（`head = ("a" "b")` 再放进列表），直接写在 `[...]` 里会报错。既有代码里 `"以下是知识库检索到的资料…" + "\n\n".join(parts)` 用的是 `+` 拼接，两种写法都行但别混。
  - 趋势类工具的排序套路：**先 `order_by(created_at desc, id desc).limit(N)` 取最近 N 条，再 `list.reverse()`** —— 直接 `asc` 排序会取到最老的 N 条，语义就反了。
  - `final_report_json` 的判空要 `isinstance(x, dict)`：SQLAlchemy 写 Python `None` 进 JSONB 在**某些写入路径**下是 SQL NULL，但历史数据/原始 SQL 写入会落成 `null` 字面量，两种都得排掉。测试里覆盖字面量要用 `text()` 手写 INSERT 传 `'null'::jsonb`。
  - 造「有先后顺序」的测试数据时显式传 `created_at=datetime(...)`（构造参数会覆盖 `server_default`），不要依赖 `func.now()` 的提交先后 —— 同事务内时间戳可能完全一样。
  - `_SCORE_LABELS` 里 `overall` 的中文是「**整体表现**」而不是「整体」，写断言时注意别只匹配单字（本次用「整体表现 6」）。
  - 工作区里 `frontend/vite.config.ts` 的用户本地改动（后端端口 8000→8001）仍在，与本 story 无关，**未纳入本次提交**。
---

## 2026-09-16 13:05 - US-004
- What was implemented: 新增第 7 个工具 `kb_list(query)`——列知识库可见文档清单。复用 `kb_service.list_documents`（预置语料 public 全站可见 + 本人 private）取文档，块数走新下沉的 `kb_service.count_chunks_by_document` 一次 group by 查完；只渲染**元信息**（标题《》/来源 平台预置|本人上传/状态 可检索等/块数），**完全不碰 raw_text 与切块正文**；query 非空按标题不区分大小写过滤、未命中回「未找到」；上限 20 篇，超出追加「仅显示前 20 篇」；异常吞掉返回兜底话术。
- Files changed:
  - `backend/app/services/kb_service.py`（新增 `count_chunks_by_document(db, document_ids) -> dict[int, int]`；补 `func` import）
  - `backend/app/api/knowledge_base.py`（列表接口改调该 service 函数，删掉重复的 group by SQL 与不再使用的 `KBChunk` import）
  - `backend/app/services/agent/tools.py`（新增 `kb_list` 工具、`_TOOL_KB_LIMIT = 20`、`_KB_STATUS_LABELS` / `_KB_SCOPE_LABELS` 两张中文映射表；`kb_service` import 改多行；`make_tools` 返回列表尾部追加，**kb_search 仍在下标 0**；模块 docstring 工具清单补第 7 项）
  - `backend/tests/test_agent_tools.py`（清理范围加 kb 两表（public 文档按标题前缀 `agentkb_` 清）；工具清单断言改 7 个；新增 7 项 kb_list 测试）
  - `docs/Agent工具设计.md`（「易撞组合」表补 `kb_search` vs `kb_list` 的划线：内容 vs 清单，且 kb_list 绝不返回正文）
  - `scripts/ralph/prd.json`（US-004 passes=true）
- Quality checks: `ruff check .` all passed；`pytest -q` 168 passed（US-003 时 161，本次 +7）；`pytest tests/test_agent_tools.py` 38 passed。全量跑完 kb 表被清空，已 `python -m scripts.seed_kb_preset` 恢复 7 条预置语料，并另跑脚本确认 `kb_list` 对真实预置语料的输出正常、测试残留文档为 0。
- **Learnings for future iterations:**
  - `list_documents` 的可见性条件（`_visible_clause`）对**完全无身份**（user_id 与 anonymous_id 皆空）的调用是安全的：只返回 `scope=public`，不泄露任何私有文档。所以 `kb_list` 这个「半公共」工具**不需要**像个人数据工具那样拒绝无身份调用（否则新访客连平台有哪些资料都问不出来）；`kb_search` 同理。判断标准是「返回集是否天然只有公共数据」。
  - 需要复用接口层 SQL 时**先下沉再调用**：本次把知识库列表接口里的块数 group by 下沉成 `count_chunks_by_document`，接口与工具共用一份。判断依据是 `api/knowledge_base.py` 里有没有同款查询——直接复制到 tools.py 会违反 Router → Service → Model 单向依赖（且两份 SQL 会漂移）。
  - `ISC004` 本次在**两处**踩到：工具里的多行 f-string 和测试里的多行 SQL 字符串。规律：**只要多行隐式拼接出现在 `[...]` / `(...)` 集合字面量里就报**；解法是先赋给变量（`head = (...)` / `chunk_delete = (...)`）再放进集合。测试里拼 SQL 尤其容易忘。
  - 测试数据清理的新坑：`scope=public` 的文档归属字段两者皆空，`DELETE ... WHERE anonymous_id IN (...)` 清不掉，会永久留在**开发库**里（本项目测试直连开发库）。凡是造「归属为空」的数据，必须给标题加测试前缀并按前缀清（已在 Codebase Patterns 记录）。
  - 开发库长期有 7 篇预置语料，「空库」和「精确条数」两类断言会被它干扰：空库分支用 `monkeypatch.setattr("app.services.agent.tools.list_documents", lambda *a, **k: [])` 隔离；计数断言只写与总数无关的片段（如「仅显示前 20 篇」），别写死「共有 25 篇」。
  - 工作区里 `frontend/vite.config.ts` 的用户本地改动（后端端口 8000→8001）仍在，与本 story 无关，**未纳入本次提交**。
---

## 2026-09-16 14:25 - US-005
- What was implemented: 新增第 8 个工具 `platform_help(topic)` —— 答「本平台自身功能怎么用」。整段是**纯静态文案**（函数体不查库、不调模型）：9 个主题各一段独立说明（个人中心 / 上传简历 / AI 分析 / 模拟面试 / 在线对话 / AI 客服 / 使用日志 / 数据看板 / 语料库管理），路由表 `_PLATFORM_TOPICS` 用「别名元组 → 文案」对 topic 做小写包含匹配、**别名顺序即优先级**（具体主题排前，防「客服」这类泛词抢匹配），topic 为空或全不命中回落 `_PLATFORM_OVERVIEW` 功能总览。同时给 `kb_search` docstring 补反向排他句（平台功能问题改用 platform_help），`platform_help` docstring 反向点名 kb_search。
- Files changed:
  - `backend/app/services/agent/tools.py`（新增 10 个静态文案常量 `_PLATFORM_OVERVIEW` + 9 个 `_PLATFORM_*`、路由表 `_PLATFORM_TOPICS`、助手 `_platform_reply`、`platform_help` 工具；`make_tools` 返回列表尾部追加，**kb_search 仍在下标 0**；`kb_search` docstring 补排他句；模块 docstring 工具清单补第 8 项）
  - `backend/tests/test_agent_tools.py`（工具清单断言改 8 个并改名；无身份用例补注释说明 kb_search / platform_help 为何不在此列；新增 12 项 platform_help 测试）
  - `docs/Agent工具设计.md`（§二 `platform_help` 行补已实现要点：静态文案 + topic 路由 + 别名优先级）
  - `scripts/ralph/prd.json`（US-005 passes=true）
- Quality checks: `ruff check .` all passed；`pytest -q` 180 passed（US-004 时 168，本次 +12）；`pytest tests/test_agent_tools.py` 50 passed。全量跑完 kb 表被清空，已 `python -m scripts.seed_kb_preset` 恢复 7 条预置语料。另用真机脚本抽查 6 句口语化问法，路由符合预期。
- **Learnings for future iterations:**
  - **证明「纯静态工具不查库」的最省事写法**：`make_tools(None, None, None, ToolContext())` 再 invoke —— `db` 传 `None` 都不炸，就说明函数体确实没碰 db（比 mock 更直接）。反过来说，静态工具里**绝不能**引用闭包的 `db`，否则这个测试会立刻红。
  - 路由表唯一要操心的是**匹配顺序**：「别名顺序即优先级」，越具体的主题排越前。加别名前先自问「这句话会不会命中更靠前的主题」——比如给「上传简历」加裸「上传」就会抢走「语料库管理」的「上传文档」；同理别加裸「面试」（会抢走个人中心/使用日志里提到面试的问法）。
  - 帮助类文案的准确性必须回前端核对**入口层级与可见性**：第一版我写「首页右下角也有悬浮入口」，核对 `App.vue` 才发现 `AgentWidget` 是 `v-if="isAdmin"`（管理端专属），用户端只有左侧导航整页入口。写这类文案前先 grep 导航项与入口的 `v-if` 条件。
  - 文案里的数字/上限从 `settings` 取（如 `settings.max_interview_turns`）而不是写死字面量，否则改配置后文案会撒谎；本项目硬事实来源：上传限 5MB/5 页仅文本 PDF（`app/api/resumes.py` + `config.upload_max_size/_max_pages`）、单场面试 10 轮（`config.max_interview_turns`）、四维评分中文名在 `_SCORE_LABELS`。
  - 断言「命中主题 vs 回落总览」用总览里的**独有标记**（本次用 `_OVERVIEW_MARK = "主要功能："`），比断言整段文案好维护；主题用例用 `@pytest.mark.parametrize` 一次覆盖 9 个主题。
  - 工作区里 `frontend/vite.config.ts` 的用户本地改动（后端端口 8000→8001）仍在，与本 story 无关，**未纳入本次提交**。
---

## 2026-09-16 15:00 - US-006
- What was implemented: 工具描述互斥性校准（**纯 docstring 改动，不动任何行为逻辑**）。8 个工具的 docstring 统一改成四段式——「一句摘要（做什么）→ 空行 → `什么时候用：` → `什么时候不用：` → 入参说明」，两个中文冒号小标题作为机器可校验锚点。原本缺「什么时候不用」的三个工具补齐：`resume_lookup`（分析结论 → analysis_read）、`interview_history`（跨场趋势 → score_trend、功能入口 → platform_help）、`usage_stats`（入口说明 → platform_help、条数 → resume_lookup/interview_history）。互指覆盖：kb_search ↔ platform_help、resume_lookup ↔ analysis_read、kb_search ↔ kb_list、score_trend ↔ interview_history、platform_help ↔ 个人数据四件套。
- Files changed:
  - `backend/app/services/agent/tools.py`（8 个工具 docstring 重写为四段式；模块 docstring「设计要点」补一条描述规范；函数体一行未动）
  - `backend/tests/test_agent_tools.py`（新增 13 项：8 项参数化三段式断言、4 项参数化易撞互指、1 项「清单 == make_tools 返回」一致性断言）
  - `docs/Agent工具设计.md`（§四 描述写作规范补「落地口径」：四段式模板 + 两条守门测试；易撞组合表从 4 对扩到 6 对）
  - `scripts/ralph/prd.json`（US-006 passes=true）
- Quality checks: `ruff check .` all passed；`pytest -q` 193 passed（US-005 时 180，本次 +13）；`pytest tests/test_agent_tools.py` 63 passed。全量跑完 kb 表被清空，已 `python -m scripts.seed_kb_preset` 恢复 7 条预置语料。另用脚本打印 8 个工具的 `description`，逐条确认长度 218~357 字符且两段锚点齐全。
- **Learnings for future iterations:**
  - **描述要能被测试断言，就得有固定锚点**：自由段落（如原来的「当用户问……时调用」）没法断言，统一用「什么时候用：」「什么时候不用：」两个中文冒号小标题后，测试只写 `in` 判断即可，以后加工具自动被守门覆盖。
  - 光断言「长度 > 50」会漏掉「加了工具但没补描述」的情况——必须再加一条**集合相等**测试（`set(_descriptions()) == set(_ALL_TOOLS)`），否则新工具悄悄逃过校准。
  - 取工具描述**不需要数据库**：`make_tools(None, None, None, ToolContext())` 直接读 `.description`，比建 db session 快，也顺带说明描述是静态的（与 US-005「db 传 None 证明静态」同一手法）。
  - 写「什么时候不用」时最容易漏的是**半公共撞车**：`usage_stats`（本人真实用量数字）vs `platform_help`（"在哪看用量"的入口说明）就是典型，两者字面都含「用量」但语义完全不同；同理 `platform_help` 与四个个人数据工具（静态说明 vs 真实数据）也要划清。
  - 改 docstring 也会影响 LangChain 的行为契约：`@tool` 默认把**整个 docstring** 当 `description`（本项目没有 Google 风格 `Args:` 段，所以不会被拆分）。写描述时别引入 `Args:` 之类的分段标题，否则描述被截断、入参说明也丢了。
  - 工作区里 `frontend/vite.config.ts` 的用户本地改动（后端端口 8000→8001）仍在，与本 story 无关，**未纳入本次提交**。
---
  - 工作区里 `frontend/vite.config.ts` 的用户本地改动（后端端口 8000→8001）仍在，与本 story 无关，**未纳入本次提交**。
---

## 2026-09-16 16:10 - US-007
- What was implemented: 工具路由评测集 + 脚本就绪（8 工具口径）。`data/agent_eval/routing.json` 24 条口语化用例（8 个工具各 3 条，字段 question/expected_tool，题面与 US-006 校准后的描述边界一一对应）。`backend/scripts/eval_agent_routing.py` 用真实工具 schema（`make_tools(None, None, None, ToolContext())` 只取描述）+ `build_chat_llm().bind(temperature=0).bind_tools(tools)`，每题**一次** LLM 调用，读返回消息首个 `tool_calls` 的工具名——**工具执行体全程不运行**（不查库、不检索）。产出 top-1 准确率 / 分工具命中率 / 混淆矩阵（期望 × 实际）/ 逐题明细 / 误选清单，写入 `data/agent_eval/report.md`。LLM 未配置（ValueError）或调用失败（402/超时）时以**退出码 2 中止且不写报告**，避免空报告被当成评测结果。可测性上把「一次 LLM 调用」抽成注入的 `ask(question) -> BaseMessage`，`run/summarize/confusion_matrix/build_report` 全是纯函数。
- Files changed:
  - `data/agent_eval/routing.json`（新增，24 条用例）
  - `backend/scripts/eval_agent_routing.py`（新增评测脚本）
  - `backend/tests/test_eval_agent_routing.py`（新增 10 项单测）
  - `data/agent_eval/report.md`（真机跑通的基线报告，24/24 = 100%）
  - `docs/Agent工具设计.md`（新增 §五「工具路由评测（准确率怎么量）」，原待办口径顺延为 §六）
  - `AGENTS.md`（启动命令补 eval_agent_routing；v3.5 约定补「改工具描述/增删工具后必须跑路由评测」与 8 工具基线 24/24）
  - `scripts/ralph/prd.json`（US-007 passes=true）
- Quality checks: `ruff check .` all passed；`pytest -q` 203 passed（US-006 时 193，本次 +10）；`pytest tests/test_eval_agent_routing.py` 10 passed。全量跑完 kb 表被清空，已 `python -m scripts.seed_kb_preset` 恢复 7 条预置语料。真机跑通 `python -m scripts.eval_agent_routing`：DeepSeek 通道正常，8 工具口径 **24/24 = 100%**。
- **Learnings for future iterations:**
  - **ruff 的启用规则集比默认多**：`RUF100`（多余 noqa）/ `BLE001`（盲吞异常）/ `B008` / `SIM` 都在生效，但 **`E402` 没启用**。照 `scripts/eval_rag.py` 抄「`sys.path.insert` 后再 import」时**别加 `# noqa: E402`**，会直接被 RUF100 判红；吞异常照既有写法 `except Exception:  # noqa: BLE001  原因`。
  - 路由评测不需要跑工具：`make_tools` 的返回值本身就是 LangChain 工具对象（带 schema 与描述），bind 给模型后读 `msg.tool_calls[0]["name"]` 即可；**db 传 None 也不会被执行**，脚本因此完全不碰业务数据。
  - 让 CLI 脚本可测的关键是**把 LLM 调用抽成参数**：`run(cases, ask)` 里 `ask` 是 `(question) -> BaseMessage`；测试用替身模型（只需 `bind` / `bind_tools` / `invoke`）就能离线覆盖统计与报告。测 `main()` 则 monkeypatch `load_cases` + `REPORT_PATH`(tmp_path) + `build_chat_llm` 三件套，顺势把「LLM 挂了要非零退出且不落报告」也测了。
  - 评测类脚本产出必须**防空报告**：任何中止路径（未配置 Key、调用异常）都要在不写文件的前提下退出非零，否则后一轮会拿空报告当基线对比。
  - `AIMessage` 没走工具时 `tool_calls` 是空列表，要落一个 `NO_TOOL` 占位计入未命中，否则「模型直接回答」会被漏统计、准确率虚高。
  - 真机跑通的**同时**把报告写进仓库，US-012 扩到 11 工具时可直接对比这条 8 工具基线（100%）。
---

## 2026-09-16 11:35 - US-008
- What was implemented: 工具内 LLM 调用的**独立限额与记账**（批次 2 的硬前置，本 story 不新增工具）。`config.py` 新增 `daily_agent_tool_llm_limit: int = 20`（`.env.docker.example` 同步 `DAILY_AGENT_TOOL_LLM_LIMIT=20`）；`tools.py` 新增模块级统一入口 `_run_tool_llm(db, user_id, anonymous_id, call)`：查当日 `agent_tool_llm` 次数 → 达到上限（含 `limit=0`，等于关闭）就 **不执行 call** 直接返回「今日工具内 AI 调用已达上限」话术 → 放行则执行并记一条带 token 合计的 `agent_tool_llm` 用量后 commit。辅助函数 `_tokens_total(result)` 用 `getattr` 取 `AnalysisResult` 的 prompt/completion 合计（非该类型记 None），`_record_tool_llm_usage(...)` 专管记账。计数/写入复用 `usage_service.count_today_usage_by_owner` / `write_usage`，**没写新 SQL，也没动 `daily_agent_limit` 的判定**。`_ACTION_LABELS` 补 `agent_tool_llm → 工具内 AI 调用`，避免用量统计把英文枚举甩给用户。
- Files changed:
  - `backend/app/core/config.py`（新增 `daily_agent_tool_llm_limit=20`）
  - `backend/.env.docker.example`（同步该配置项）
  - `backend/app/services/agent/tools.py`（导入 `Callable`/`TypeVar`/usage_service 两函数；新增 `_TOOL_LLM_ACTION` / `_TOOL_LLM_LIMIT_REPLY` 常量、`_T` TypeVar、`_tokens_total` / `_record_tool_llm_usage` / `_run_tool_llm` 三个助手；`_ACTION_LABELS` 补一条；模块 docstring「设计要点」补一段）
  - `backend/tests/test_agent_tools.py`（新增 9 项：未超限执行并记账、未达上限（=上限-1）放行、恰好达上限与超限（参数化 3/5）拦下且不调模型不多记账、limit=0 关闭、限额查询抛错兜底不抛、记账写库失败不连累结果、无身份可跑但不写不可归因的账、usage_stats 显示中文动作名）
  - `docs/Agent工具设计.md`（§六「待办口径」改写为已实现的「工具内 LLM 调用的限额与记账」）
  - `AGENTS.md`（v3.5 约定补「工具内部要调模型的必须走 tools._run_tool_llm」）——**该文件被 `.gitignore:29` 忽略，属本地手册，改动不会进提交**
  - `scripts/ralph/progress.txt`（Codebase Patterns 修正 BLE001 误传 + 新增两条工具限额/记账模式）
  - `scripts/ralph/prd.json`（US-008 passes=true）
- Quality checks: `ruff check .` all passed；`pytest -q` 211 passed（US-007 时 203，本次 +8 后追加 1 项 → `test_agent_tools.py` 63 → 72）；`pytest tests/test_agent_tools.py` 72 passed。全量跑完 kb 表被清空，已 `python -m scripts.seed_kb_preset` 恢复 7 条预置语料（111 块），并复查开发库 `agent_tool_llm` 残留行数 = 0。
- **Learnings for future iterations:**
  - ⚠️ **修正 US-002/US-007 里"吞异常一律加 `# noqa: BLE001`"的说法——那条是错的**，照做会立刻被 `RUF100` 判为多余指令。实测规律：`BLE001`（盲吞异常）**只要处理器带堆栈地记了日志就免报**——`logger.exception(...)` 或 `logger.warning(..., exc_info=True)` 都不报；纯 `return`/赋值、或 `logger.warning("x")`（无 `exc_info`）才报（`app/main.py:89`、`app/db/session.py:38` 是后者，所以才要 noqa）。tools.py 里惯用的 `except Exception:` + `logger.exception(...)` + `return 文案` **不要加 noqa**。判据永远是当场跑 `ruff check <文件>`，别照抄别处（已在 Codebase Patterns 记录）。
  - `ruff check --select BLE001` 单独跑会**什么都报不出来**（`--isolated` 也一样），必须跑项目配置下的完整 `ruff check <文件>` 才看得到真实结果——排查规则行为时别用 `--select` 缩小范围。
  - 记录一个排查方法：想知道某条规则到底什么时候触发，就**在 backend/ 下造一个探针 .py**（放 5 个变体函数）跑 `ruff check ./_probe.py --output-format concise`，看行号定位，比读文档快且准。
  - **限额闸门要"省钱的关闸"**：超限时**连 `call` 都不执行**（测试里用 `calls == []` 断言），只查库不调模型；否则"限额"只是话术、钱照花。
  - 记账是旁路：`write_usage` + `db.commit()` 包在 `try/except` 里，失败只告警 + `db.rollback()`，**不能让记账故障毁掉工具结果**（与 `app/api/agent.py` 的 `_log_usage` 同一取舍，但工具层得自己 commit——Agent 主循环可能随后 fatal，不 commit 这笔账就丢了）。
  - **无归属者（user_id 与 anonymous_id 皆空）不写账**：`count_today_usage_by_owner` 对空身份返回 0（永不超限），`write_usage` 却会写出一条两者皆空的日志，既没法归因又会污染全站总量统计。这类行还有一个副作用：测试 `_purge` 按 `anonymous_id IN (...)` 清不掉，会永久留在开发库（与 US-004 的 public 文档同一类坑）。
  - 「哪些工具不需要身份」的判据这次又多一个反例：`platform_help`（静态）/`kb_search`/`kb_list`（只含公共语料）之外，**`question_gen` 这类不查个人数据但会调模型的工具也不需要身份**——所以 `_run_tool_llm` 第一参数就显式收 `user_id, anonymous_id`，不套 `_owner_filter`。
  - 新增 `action_type` 一定要同步 `_ACTION_LABELS`，否则 `usage_stats` 输出的就是英文枚举；这条已写进 Codebase Patterns。
  - 工作区里 `frontend/vite.config.ts` 的用户本地改动（后端端口 8000→8001）仍在，与本 story 无关，**未纳入本次提交**。
---

## 2026-09-16 17:20 - US-009
- What was implemented: 新增第 9 个工具 `job_match(jd_text)` —— 拿用户贴的岗位 JD 与本人简历做匹配分析（第一个工具内调 LLM 的工具）。链路：身份闭包 → 空 JD 直接要补全（不调模型）→ 取本人最新一份简历（`deleted_at is None` + `created_at desc, id desc` limit 1）→ 无简历/简历无正文给引导话术 → JD 截 4000 字、简历正文截 6000 字 → `chat_json(JOB_MATCH_SYSTEM_PROMPT, build_job_match_prompt(jd, body), settings, JobMatchReport.model_validate)` 走结构化校验与重试，整段经 `_run_tool_llm`（限额 + 记 `agent_tool_llm`）并自己 try/except 兜话术 → 渲染「整体匹配度 N/100 + 已命中关键词 + 缺失关键词 + 针对性建议」，JD 被截断时追加说明。提示词与版本常量放 `app/services/prompts.py`（`JOB_MATCH_PROMPT_VERSION`，与 analyses 表口径的 `PROMPT_VERSION` 独立），输出契约 `JobMatchReport` 放新建的 `app/schemas/agent.py`。
- Files changed:
  - `backend/app/services/prompts.py`（新增 `JOB_MATCH_PROMPT_VERSION` / `JOB_MATCH_SYSTEM_PROMPT` / `build_job_match_prompt`，模块 docstring 说明版本为何独立）
  - `backend/app/schemas/agent.py`（新增：Agent 工具内 LLM 输出的数据合同，首个模型 `JobMatchReport`）
  - `backend/app/services/agent/tools.py`（新增 `_TOOL_JD_CHARS` / `_TOOL_MATCH_RESUME_CHARS` / `_TOOL_MATCH_ITEMS` 常量与 `job_match` 工具；import `chat_json` / `JobMatchReport` / 提示词三件套；`make_tools` 尾部追加，**kb_search 仍在下标 0**；模块 docstring 工具清单补第 9 项）
  - `backend/tests/test_agent_tools.py`（清单断言 8→9 并改名、无身份用例补 job_match、`_ALL_TOOLS` 补 job_match；新增 6 项 job_match 测试 + `_patch_chat_json` / `_fake_match_result` 替身）
  - `data/agent_eval/routing.json`（补 3 条 job_match 问法，24→27 条，守门测试要求每工具 ≥3 条）
  - `docs/Agent工具设计.md`（§二 job_match 行改为已实现要点；§四易撞组合表补 job_match vs analysis_read / resume_lookup）
  - `scripts/ralph/progress.txt`（Codebase Patterns 补 5 条）
  - `scripts/ralph/prd.json`（US-009 passes=true）
- Quality checks: `ruff check .` all passed；`pytest -q` 219 passed（US-008 时 211，本次 +8）；`pytest tests/test_agent_tools.py` 79 passed。全量跑完 kb 表被清空，已 `python -m scripts.seed_kb_preset` 恢复 7 条预置语料。真机校验：用真实 DeepSeek 调了一次 `chat_json` + `JobMatchReport`，返回 match_score=80、命中/缺失关键词与 6 条建议，提示词与 schema 契约无误。
- **Learnings for future iterations:**
  - ⚠️ **新增工具必须同步补路由评测用例**：`tests/test_eval_agent_routing.py::test_cases_cover_every_tool` 断言「用例数 ≥ 工具数 × 3」，我只加工具不加用例时全量 pytest 直接红（24 >= 9*3）。US-010 加 question_gen、US-011 加 answer_review 时都要先补 3 条，否则 CI 绿不了。
  - **别为了"新增提示词"递增 `PROMPT_VERSION`**：那个常量是 analyses 表的留档口径，一递增库里所有旧分析报告都变"版本过期"，`analysis_read` 会集体返回「还没有分析报告」。本项目每套提示词各有一个版本常量（详见 Codebase Patterns），job_match 用新的 `JOB_MATCH_PROMPT_VERSION`。
  - **pydantic 的 `Model.model_validate` 不能 `is` 比较**：`assert seen[0]["validator"] is JobMatchReport.model_validate` 会失败（提示 "model_validate is model_validate"——因为每次取属性都是新的 bound 对象）。改成**调用它**并 `isinstance(结果, JobMatchReport)`。
  - 测"工具内调 LLM"的最省事替身是 monkeypatch `app.services.agent.tools.chat_json`（不是 `app.services.ai_client.chat_json`——tools 是 `from ... import chat_json`，改 monkeypatch 目标要注意导入方式）。替身记录 `system`/`user`/`validator` 三个入参，就能顺手断言"走了哪套提示词""入参截到多少字""用的是哪个校验器"。
  - 短路路径（空参数 / 无数据 / 超限）最有力的断言是 **`seen == []`**：证明一次模型都没调，比断言返回文案更能防住"限额只是话术、钱照花"的回归。
  - LangChain `@tool` 的入参由模型现填，`jd_text` 可能传空白串：`(jd or "").strip()` 判空后再决定调不调模型，别直接拿 `if not jd_text`。
  - 本次**没有**重跑 `scripts/eval_agent_routing`：报告 `data/agent_eval/report.md` 仍是 8 工具 24 条基线（US-012 会做 11 工具 33 条的全量跑与对比）。
  - 工作区里 `frontend/vite.config.ts` 的用户本地改动（后端端口 8000→8001）仍在，与本 story 无关，**未纳入本次提交**。
---

## 2026-09-16 18:20 - US-010
- What was implemented: 新增第 10 个工具 `question_gen(topic, position_type)` —— 围绕某主题出一组模拟面试题，**先检索平台知识库再依据语料出题**（不是纯模型生成）。链路：空 topic 直接要补全（不调模型）→ 走新的公共检索入口 `_kb_retrieve` 混合检索 → 命中则取前 3 块、每块截 300 字拼成资料送进提示词 → `chat_json(QUESTION_GEN_SYSTEM_PROMPT, build_question_gen_prompt(主题, 难度定位, 资料), settings, QuestionGenReport.model_validate)`，整段经 `_run_tool_llm`（限额 + 记 `agent_tool_llm`）并自己 try/except 兜话术 → 渲染「难度定位 + 3~5 道编号题」，命中时追加《出题依据》文档名与"想看答案可以让我检索知识库"，未命中/检索失败时明确标注「平台知识库未收录该主题（或检索暂时不可用），以下题目不来自平台知识库」。难度定位走新提的模块级 `_POSITION_LABELS`（intern/fresh/senior，空值与非法值回落 `_POSITION_DEFAULT="通用"`，输出里始终体现），`interview_history` 里原本局部的岗位类型映射改为复用同一份。提示词与版本常量 `QUESTION_GEN_PROMPT_VERSION` 在 `prompts.py`，输出契约 `QuestionGenReport`（`min_length=3 / max_length=5`）在 `schemas/agent.py`。
- Files changed:
  - `backend/app/services/prompts.py`（新增 `QUESTION_GEN_PROMPT_VERSION` / `QUESTION_GEN_SYSTEM_PROMPT` / `build_question_gen_prompt`；模块 docstring 说明工具提示词版本各自独立）
  - `backend/app/schemas/agent.py`（新增 `QuestionGenReport`）
  - `backend/app/services/agent/tools.py`（新增 `_kb_retrieve` 公共检索入口并让 `kb_search` 改调它、`_POSITION_LABELS`/`_POSITION_DEFAULT`/`_KB_EMBED_ERROR`/`_KB_SEARCH_ERROR`/`_TOOL_QUESTION_CHUNKS`/`_TOOL_QUESTION_LIMIT` 常量、`question_gen` 工具；`make_tools` 尾部追加，**kb_search 仍在下标 0**；`kb_search` docstring 补"要出一组模拟面试题请用 question_gen"；模块 docstring 工具清单补第 10 项）
  - `backend/tests/test_agent_tools.py`（清单断言 9→10 并改名、`_ALL_TOOLS` 补 question_gen、互指断言补 `kb_search ↔ question_gen`；新增 12 项 question_gen 测试 + `_patch_kb_hits` / `_kb_hit` / `_fake_question_result` 替身）
  - `data/agent_eval/routing.json`（补 3 条 question_gen 问法，27→30 条）
  - `docs/Agent工具设计.md`（§二 question_gen 行改为已实现要点；§四易撞组合表补 `kb_search` vs `question_gen`）
  - `scripts/ralph/progress.txt`（Codebase Patterns 补 4 条）
  - `scripts/ralph/prd.json`（US-010 passes=true）
- Quality checks: `ruff check .` all passed；`pytest -q` 235 passed（US-009 时 219，本次 +16）；`pytest tests/test_agent_tools.py` 95 passed。全量跑完 kb 表被清空，已 `python -m scripts.seed_kb_preset` 恢复 7 条预置语料。真机校验：两次真实 DeepSeek 调用（命中路径 5 道题 + 出题依据《java_interview》《system_design》标注正常；未收录路径因混合检索仍返回低分材料而未触发，见下）。真机产生的 2 条 smoke usage_logs 已按 anonymous_id 清理。
- **Learnings for future iterations:**
  - ⚠️ **同一文件上的多个 Edit 千万别并行发**：本次在 tools.py 与 test_agent_tools.py 上各踩一次——两个 Edit 同批发出，其中一个静默丢失（工具回报成功、文件没变），直到 ruff 报 F821、pytest 报互指断言失败才发现。改同一文件必须**串行逐个改**，改完 grep 关键字确认。
  - **工具里要用另一个工具的检索能力，别去 invoke 工具对象**：把检索链路抽成模块级公共入口（`_kb_retrieve` 返回 `(命中块, 兜底话术)` 而非抛异常），调用方各自决定处置——kb_search 要区分"向量模型挂了"与"检索出错"两种话术，question_gen 一律当"未收录"退回模型出题。既复用了逻辑，也没破坏 kb_search 的既有行为（`tests/test_agent.py` 里 monkeypatch `embed_texts`/`search_chunks` 的用例照常通过）。
  - **混合检索不套 `kb_min_similarity`（设计如此）**，所以真机上"未命中"分支几乎触发不了——连「量子纠缠挖矿」这种主题都返回了 `algorithm_interview` 的低分块，输出照常标"依据平台知识库出题"。这不是 bug（hybrid 就是为救回纯词法命中），但意味着"未命中"只能靠 mock 空结果覆盖；US-012 若关心这个，得单独评估要不要给工具侧加相关性门槛。
  - 岗位类型这类**跨工具/跨接口复用的中文映射**，提到模块级常量（本次 `_POSITION_LABELS`）比在两处各写一份 dict 好：`interview_history` 与 `question_gen` 的口径天然要一致，写两份迟早漂移。
  - 题目数这种"3~5 道"的硬约束直接写进 pydantic 契约（`min_length=3, max_length=5`）而不是只在提示词里说：`chat_json` 的重试机制会替你兜住模型的偷懒，渲染侧再截一次是双保险（与 `_as_items` 的用法一致）。
  - 工作区里 `frontend/vite.config.ts` 的用户本地改动（后端端口 8000→8001）仍在，与本 story 无关，**未纳入本次提交**。
---

## 2026-09-16 19:20 - US-011
- What was implemented: 新增第 11 个工具 `answer_review(question, answer)` —— 点评用户贴的一段面试回答。链路：两个入参都必填（先判 answer 空→「把他自己的回答贴进来」，再判 question 空→「把题目一起发过来」，短路时一次模型都不调）→ 回答截到 2000 字（`_TOOL_ANSWER_CHARS`），截断在输出里说明 → `chat_json(ANSWER_REVIEW_SYSTEM_PROMPT, build_answer_review_prompt(题目, 回答), settings, AnswerReviewReport.model_validate)`，整段经 `_run_tool_llm`（限额 + 记 `agent_tool_llm`）并自己 try/except 兜话术 → 渲染「10 分制三项评分（技术深度/表达结构/项目真实性，共用 `_SCORE_LABELS`、跳过 overall）+ 2~4 条改进建议」。提示词与版本常量 `ANSWER_REVIEW_PROMPT_VERSION` 在 `prompts.py`，输出契约 `AnswerReviewReport`（评分 `ge=1/le=10`、建议 `min_length=2 / max_length=4`）在 `schemas/agent.py`。与 `question_gen` 双向点名（**出题用 question_gen，点评已写好的回答用 answer_review**）。
- Files changed:
  - `backend/app/services/prompts.py`（新增 `ANSWER_REVIEW_PROMPT_VERSION` / `ANSWER_REVIEW_SYSTEM_PROMPT` / `build_answer_review_prompt`；模块 docstring 补 answer_review）
  - `backend/app/schemas/agent.py`（新增 `AnswerReviewReport`）
  - `backend/app/services/agent/tools.py`（新增 `_TOOL_ANSWER_CHARS` / `_TOOL_ANSWER_SUGGESTIONS` 常量与 `answer_review` 工具；import 补 `AnswerReviewReport` 与提示词三件套；`question_gen` docstring 补"要点评回答请用 answer_review"；`make_tools` 尾部追加，**kb_search 仍在下标 0**；模块 docstring 工具清单补第 11 项）
  - `backend/tests/test_agent_tools.py`（清单断言 10→11 并改名、`_ALL_TOOLS` 补 answer_review、互指断言补 `question_gen ↔ answer_review`；新增 8 项 answer_review 测试 + `_fake_review_result` 替身）
  - `data/agent_eval/routing.json`（补 3 条 answer_review 问法，30→33 条，满足 11 工具 ×3 的守门断言）
  - `docs/Agent工具设计.md`（§二 answer_review 行改为已实现要点；§四易撞表补 `question_gen` vs `answer_review`；§五用例集口径改 11 工具 33 条；§六提示词版本常量补全）
  - `scripts/ralph/progress.txt`（Codebase Patterns 补 5 条）
  - `scripts/ralph/prd.json`（US-011 passes=true）
- Quality checks: `ruff check .` all passed；`pytest -q` 245 passed（US-010 时 235，本次 +10）；`pytest tests/test_agent_tools.py` 105 passed。全量跑完 kb 表被清空，已 `python -m scripts.seed_kb_preset` 恢复 7 条预置语料。真机校验：一次真实 DeepSeek 调用，三项评分（6/5/5）与 4 条具体建议输出正常，schema 契约无误。
- **Learnings for future iterations:**
  - ⚠️ **`prompts.py` 是全仓库唯一的 CRLF 文件**：给它做多行 Edit 时 old_string **匹配不上**（行尾 `\r\n`），只能拿单行当锚点插入（已写进 Codebase Patterns）。以后凡是"Edit 报找不到字符串"而肉眼看着明明一样，先怀疑行尾。
  - **"入参必填"类工具的短路顺序决定话术**：answer_review 先查 answer（没有回答就根本没法点评），再查 question。两个参数都空时给的是 answer 的话术，写测试要按这个顺序断言。
  - **截断测试的填充字不能撞提示词模板**：第一版用「答」填 2800 字，断言 `count("答") == 2000` 失败（模板里"候选人的回答"也含「答」，实际 2001）。换成模板里不存在的字即可（已写进 Codebase Patterns）。
  - 复用四维评分时**遍历 `_SCORE_LABELS` 再 `if key != "overall"`**，比新写一份三项中文映射稳——模拟面试报告的口径一改，工具这边自动跟着走。
  - 本工具**不需要身份**（题目与回答都是用户当场贴的，不查任何个人数据），所以没有 `_owner_filter`；但仍必须走 `_run_tool_llm` 限额与记账——"不查库"不等于"不花钱"。
  - 工作区里 `frontend/vite.config.ts` 的用户本地改动（后端端口 8000→8001）仍在，与本 story 无关，**未纳入本次提交**。
---
