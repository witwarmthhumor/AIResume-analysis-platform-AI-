# AI 简历分析 + AI 模拟面试 — 后续开发规划

> **本文件是干嘛的**：项目后续方向的唯一权威规划——现状盘点、问题清单、优先级、推进顺序。
> 建立 2026-09-14 · **最后更新 2026-09-19（v3.6 收口后复盘 + 方向决策，批次重排为 v3.7/v3.8/v3.9）**。
> 权威需求以 [PROJECT-PLAN.md](../PROJECT-PLAN.md) 为准；工具设计的对照说明见 [Agent工具设计.md](./Agent工具设计.md)。

---

## 0. 结论先行

**功能层面已经跑在规划前面，工程收口落在后面。**

上一轮规划的第 3 批（E1~E4）已通过 Ralph 自动循环全部落地并超额完成（工具 4→11 + 路由评测）；第 1 批（收口止血）已于 2026-09-18 完成 4/5（v3.6）。**2026-09-19 复盘盘点（三路：规划文档 / 工程欠账 / 产品功能面）结论：第 2 批工程门禁 8 项仍全部未做，且新发现 prod 部署配置三处缺陷、前端零测试零路由、venv 混入可疑包 httpx2 等欠账。**

与 owner 确认的三个方向决策（2026-09-19）：
1. **下一批主攻工程质量门禁**（v3.7，承接原第 2 批并扩容）；
2. **批准推送远端 + 上 GitHub Actions CI**（v3.7 内执行：push main + tags，远端仓库已在 GitHub）；
3. **暂无云服务器**：线上演示降级为「本地一键演示模式」（v3.8），云部署后置。

推进顺序：**v3.7 工程门禁 → v3.8 本地演示 → v3.9 功能演进（按兴趣）**。

---

## 1. 上一轮规划的完成情况

| 批次 | 规划内容 | 状态 |
|---|---|---|
| 第 1 批 | D4 配置收口 · B 多 provider · A 线上演示 | 🟢 **2026-09-18 收口完成 4/5**（1.2 推远端保持待确认，本轮明确不推） |
| 第 2 批 | C 工程质量基线 · D1~D3 文档升版 | 🟡 D1~D3 已做；**C 全部未做**（无 CI / 无覆盖率 / 无依赖锁定） |
| 第 3 批 | E1 Agent 多工具 · E2 RAG 提优 · E3 复盘雷达图 · E4 分析版本对比 | ✅ **已完成且超额**（Ralph 13 个 story：工具 4→11、混合检索 hit@1 87.8%、路由评测 33/33） |

---

## 2. 本次体检发现（2026-09-18）

### P0 · 必须优先处理

| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| 1 ✅已解决（v3.6） | **Ralph 成果在未合并的分支上** | `ralph/agent-tool-expansion` 领先 `main` **13 个提交**（18 文件 +3614 行）；`main` 停在 v3.5 的 `2e45707` | 已 fast-forward 合并，HEAD=6424543 |
| 2 ✅已批准（2026-09-19） | **远端落后更多** | v3.6 收口后本地领先 `origin/main` **20 个提交** | 已批准在 v3.7 推送 main + tags |
| 3 ✅已解决（v3.6） | **v3.5 及之后没有 tag** | tag 最新是 `v3.4` | 已补打 v3.5（→2e45707）与 v3.6（→8e35527） |
| 4 ✅已解决（v3.6） | **数据库不可用时业务请求挂起** | 实测：容器停 → `POST /api/auth/register` 挂 60 秒以上无响应 | 已加 connect_timeout/pool_timeout + 全局 503，实测 5.3s 返回 |
| 5 ✅已缓解（v3.6） | **容器闲置后会自行退出** | 三容器 `Exited (0) 15 hours ago` | check_env 自检可一次性发现并给修复命令（ollama 假死实际抓到过） |

### P1 · 一致性与整洁度

| # | 问题 | 证据 |
|---|---|---|
| 6 | **README 未更新到 11 工具** | `grep -c '11 个工具\|job_match\|eval_agent_routing' README.md` = **0**，仍停在 4 工具口径 |
| 7 | **技术文档同样未更新** | `docs/tech-stack-and-features.md` 同上，计数 0 |
| 8 | **进度日志缺 v3.5+ 记录** | `PROGRESS.md` 已补 v3.6 收口段（09-18），但 13 个 Ralph story 无专条 → v3.7·7.1 |
| 9 | **ruff format 漂移扩大到 24 文件** | `ruff format --check` 报 24 个待重排（v3.5 时是 18 个）——Ralph 新代码未格式化 |
| 10 ✅已解决（v3.6） | **端口不一致未收口** | `vite.config.ts` 代理指向 **8001**（未提交），而 README / AGENTS.md / nginx.conf / compose 全按 **8000** |
| 11 ✅已解决（v3.6） | **`.workbuddy/` 未被忽略** | `git check-ignore` 未命中，工作区记忆有误提交风险 |
| 12 ✅已解决（v3.6） | **Ralph 目录忽略策略不统一** | 已入库 prd.json/progress.txt/ralph.sh/watch.sh/CLAUDE.md，`.last-branch` 加入忽略 |

### P2 · 工程债

| # | 问题 | 证据 |
|---|---|---|
| 13 ✅已解决（v3.7·7.7） | **无 CI** | `.github/` 不存在，260 个测试全靠人工跑 |
| 14 ✅已记录基线（v3.7·7.8，门槛另批） | **无覆盖率门槛** | 未装 `pytest-cov` |
| 15 ✅已收口（v3.7·7.3，lock 快照；httpx2 系 langsmith 正式依赖） | **依赖漂移 + 可疑包** | 实测 `openai 2.54.0`（requirements 写 `>=1.40`）、`pgvector 0.5.0`、`pytest 9.1.1`、`ruff 0.16.5`；venv 内混入**来路不明的 `httpx2 2.12.0`** |
| 16 ✅已解决（v3.7·7.4） | **测试清理策略不统一** | 老测试（如 `test_analyses.py`）直接 `DELETE FROM analyses / usage_logs / resumes` 全表清；新测试用专属标记。本次实测因"上次被强杀的测试留下 1 条残留"导致断言失败；跑全量还会清空 `kb_*` 表 |

### 补充盘点（2026-09-19，v3.6 收口后三路探查新发现）

| # | 缺陷 / 欠账 | 证据 |
|---|---|---|
| 17 ✅已解决（v3.7·7.5） | **prod compose 的 `backend:` 服务键重复** | `docker-compose.prod.yml` 42 行与 87 行重复定义 backend，`docker compose` 解析即报错或静默覆盖——上线第一步就会踩 |
| 18 ✅已解决（v3.7·7.5，实测 413 消失） | **nginx 上传限制与后端矛盾** | `frontend/nginx.conf` 无 `client_max_body_size`（nginx 默认 1MB），后端允许 5MB，经 nginx 上传 1~5MB 简历直接 413；另缺 gzip 与安全响应头 |
| 19 🟡已文档化挂账（v3.7·7.5，README 标注单 worker 限定） | **登录防爆破为进程内存计数** | `config.py` 注释自认"多 worker 部署需换 Redis 集中计数"；当前 uvicorn 单 worker 下无害，多 worker 即失效 |
| 20 🟡挂账至 v3.9 | **前端 17 组件零测试零路由** | `package.json` 仅 dev/build/preview，无 vue-router（App.vue 单文件维护视图切换）、无任何 lint/测试 |
| 21 ✅核实为误报（v3.7·7.9） | **v0.5 tag 缺失** | ~~PROGRESS 封版清单无 v0.5~~ **已核实为误报**：`v0.5` tag 在位（→ 51b17f2 阶段4验收合并提交），仅 PROGRESS 文字未提及，锚点序列无空洞 |
| 22 ✅已解决（v3.7·7.1） | **Agent工具设计.md 口径滞后** | 文件头仍写"对应版本 v3.5（4 个工具）"，§二 4 个已实现工具行缺 ✅ 标记（内容已到 11 工具，仅标注性滞后） |
| 23 ✅已解决（v3.7·7.1） | **文档口径失真三处** | README/docs 写"四个工具 / 143 pytest"，实际 11 工具 / 260 pytest；docs 写"当前模型通义 qwen-plus"与实际 DeepSeek 矛盾 |

---

## 3. 当前健康度快照（2026-09-18 实测 · v3.6 收口终态）

| 维度 | 状态 |
|---|---|
| pytest | **260 passed**（v3.6 加 13 例：DB 快速失败 6 + 自检脚本 7）✓ |
| 覆盖率基线（7.8 记录） | 服务层（`app/services`）**89%**（1034 语句 / 缺 114）；短板：task_service 30%（Celery 胶水）、embedding_service 39%（外部 Ollama 调用）、llm_factory 56%；强项：lexical_service 99%、kb_chunker/pdf_parser 94%、agent/tools 92%——只记录不设门槛，门槛另批决策 |
| ruff check | All checks passed ✓ |
| ruff format | ⚠️ 24 文件待重排 |
| 前端构建 | ✓ 136.02 kB JS / 57.30 kB CSS |
| 数据库 | 迁移在 head（8 次）· 10 张业务表 · 预置语料 7 文档 / 111 块 |
| Agent 路由 | 33 题 / 11 工具，top-1 **100%** |
| RAG 检索 | 49 题，hit@1 **87.8%** / hit@5 **100%** |
| AI 通道 | ✓ 可用（DeepSeek 402 已解决） |

---

## 4. 后续优化方案（v3.7 / v3.8 / v3.9）

### 第 1 批 · 收口止血（P0，已完成 4/5，余项并入 v3.7·7.6）

| 任务 | 具体动作 | 验收 |
|---|---|---|
| 1.1 ✅（2026-09-18）合并并封版 | `ralph/agent-tool-expansion` 合并回 `main`；打 tag `v3.5`（混合检索+四工具）与 `v3.6`（11 工具+路由评测） | `main` 含全部提交，tag 指向正确 |
| 1.2 同步远端 → 并入 v3.7·7.6 | 推送 `main` 与 tag 到 `origin`——v3.6 收口时按旧策略未推；**2026-09-19 owner 已批准推送，由 v3.7·7.6 执行** | `origin/main` 与本地一致 |
| 1.3 ✅（2026-09-18）工作区清理 | 端口二选一（已还原 8000）；`.gitignore` 补 `.workbuddy/`；统一 `scripts/ralph/` 忽略策略 | `git status` 只剩有意保留项 |
| 1.4 ✅（2026-09-18）DB 快速失败 | `db/session.py` 加 `connect_timeout`（5s）与 `pool_timeout`（10s）；配全局异常转 503 友好话术 | 容器停时接口 5 秒内返回 503（实测 5.3s），不再挂起 |
| 1.5 ✅（2026-09-18）一键自检脚本 | 新增 `backend/scripts/check_env.py`（`python -m scripts.check_env`，支持 `--skip-ai`）：容器健康 → 迁移版本 → 端口占用 → `/health/ready` → 语料 → AI 通道探测 | 一条命令输出全部状态（实测 9/9 通过退出码 0）；启动项目前必跑 |

### v3.7 · 工程质量门禁（✅ 2026-09-19 完成 9/9；CI run 35380624972 全绿）

承接原第 2 批 8 项并扩容（补入 §2 新发现 #17~#23 的处置），方向决策已定：**门禁先行 + 推送远端 + 上 CI**。

| # | 任务 | 具体动作 | 验收 |
|---|---|---|---|
| 7.1 ✅（09-19）文档口径升版 | README + `docs/tech-stack-and-features.md` 升到 11 工具 / 260 pytest / v3.6 口径（修正 #23 的"当前模型"矛盾）；`docs/Agent工具设计.md` 头部刷新 + §二 4 行补 ✅（#22）；`PROGRESS.md` 补 Ralph 13 story 汇总条 | 三份文档与代码口径一致，grep 不再命中"四个工具" |
| 7.2 ✅（09-19）ruff format 收口 | 一次性 `ruff format .` 收掉 24 个漂移文件；加 `.pre-commit-config.yaml`（ruff check + format check）防复发。**单独提交**，全量回归后再合入 | `ruff format --check .` 全过，260 pytest + npm build 不红 |
| 7.3 ✅（09-19）依赖收口 | **卸载 `httpx2`**（全仓零引用，来路不明）+ `pip check`；用 pip-tools 从当前 venv 实测版本生成 `requirements.lock`（requirements.txt 保留宽松写法，lock 供 CI 与复现） | venv 无 httpx2；lock 入库且可复现安装 |
| 7.4 ✅（09-19）测试隔离改造 | 老测试 8 处全表 DELETE fixture 改为专属标记清理（沿 `test_agent_tools.py` 的 `_KB_PREFIX` 套路），逐文件改逐文件验证；消除 `test_rag.py` 清空 `kb_*` 的行为 | 跑全量 pytest 后业务表与 `kb_*` 不再被清空，260 用例全绿；AGENTS.md"跑完恢复语料"条目删除 |
| 7.5 ✅（09-19）prod 部署缺陷三连修（#17/#18/#19） | compose 去重 `backend:` 键；nginx 补 `client_max_body_size 6m` + gzip + 安全头；uvicorn 多 worker 暂缓，防爆破"单 worker 限定"在文档标注（#19 挂账，见 §5） | `docker compose -f docker-compose.prod.yml config` 校验通过；1~5MB 文件经 nginx 上传不再 413 |
| 7.6 ✅（09-19）推送远端 | push `main` + 全部 tags 到 `origin`（v3.6 收口时按旧策略未推，owner 2026-09-19 已批准） | `origin/main` 与本地一致 |
| 7.7 ✅（09-19）CI 上线 | `.github/workflows/ci.yml`：ruff check → pytest（services 起 pgvector + redis 容器）→ npm build。ollama 依赖优先用 actions/cache 缓存模型，兜底用环境变量在 CI 跳过 embedding 用例（执行时定）；README 加 badge | push 后 CI 全绿；检查项与本地 check_env 口径一致 |
| 7.8 ✅（09-19）覆盖率基线 | 引入 `pytest-cov` 跑一次服务层覆盖率，数字记入 docs；**只记录不设门槛**（门槛另批决策） | docs 有覆盖率基线记录 |
| 7.9 ✅（2026-09-19）v0.5 tag 缺失处置（#21） | 核实结果：`v0.5` tag 实际在位（→ 51b17f2），探索报告误报，无需补打 | 封版锚点序列无空洞 ✓ |

### v3.8 · 本地一键演示模式（中期；云部署后置）

原 3.1「线上演示闭环」是全项目唯一从未启动的里程碑；因暂无云服务器，降级为"本地可演示"，`docker-compose.prod.yml` 修好（7.5）后本地起通一遍即"准线上"。

| # | 任务 | 具体动作 | 验收 |
|---|---|---|---|
| 8.1 | 演示数据种子 | 新增 `backend/scripts/seed_demo.py`：演示账号 + 示例简历 + 一场已完成分析 + 一场面试记录 + 使用日志数据 | 一条命令后全站各页面有内容可看 |
| 8.2 | 快速体验链路 | README 首屏「快速体验」小节 + 一键演示脚本（起三容器 → 迁移 → seed → 起前后端）；用 prod compose 本地起通一遍验证 | 新人按文档 10 分钟内跑起可演示环境 |
| 8.3 | 报告导出 PDF（原 3.4） | 优先纯前端 print-CSS 轻方案，后端渲染（weasyprint 等）备选，执行时选型 | 分析报告 / 面试评价可导出 PDF |

### v3.9+ · 功能演进（远期，按兴趣）

- **面试官 Agent**（原 3.2）：**场景独立而非工具多**——需严格人格、独立长上下文、不与客服共享历史；工具仍 11 个，不构成拆多 Agent 的理由（判据见 `docs/Agent工具设计.md` §五）。
- **前端工程化**（#20 新欠账）：引入 vue-router、views/components 目录规整、vitest 冒烟——17 组件无路由无测试，重构窗口在收窄。
- **多 provider 兜底**（原 3.3）：402 已解决，维持可选；保留"换模型改三行配置"实测记录即可。
- **语音面试**：远期，消息结构已兼容（PROJECT-PLAN §0）。

---

## 5. 明确不做 / 挂账

**长期不做：**
- **不拆多 Agent / 不引 LangGraph**：工具未到互相干扰的量级；真要引 langgraph 必须锁 `>=0.3,<0.4`（默认装 1.x 会把 langchain-core 升到 1.6.3，打断现有 Agent 层）
- 不做支付 / 配额计费 / 多租户后台
- 不做移动端 App、不做国际化
- 不为"技术新颖"替换已稳定的栈（pgvector / Ollama / Vue3 均保留）
- docx / 图片简历与 OCR：不做（简历只收文本型 PDF 的边界维持）

**挂账（触发条件出现时再做）：**
- 防爆破 Redis 集中计数 + 显式 CORS 配置：仅在升级多 worker 部署或跨域部署时启用（#19，当前单 worker + 同源架构下无害）
- 云服务器线上部署：owner 有服务器资源后，从 v3.8 的本地演示模式自然升级（prod compose 与部署文档在 7.5/8.2 已备好）

---

## 6. 执行纪律

1. **一次只推一个批次**：v3.7 全部收口 → 再开 v3.8；单方向耗时超预估 1.5 倍 → 砍范围（沿用 PROJECT-PLAN §8）
2. 改提示词 → 递增 `PROMPT_VERSION` + 用 `test-resumes/` 5 份跑回归
3. 改切块 / embedding / 检索 → 必跑 `scripts/eval_rag.py` 对比基线（hit@1 87.8% / hit@5 100%）
4. 增删工具 / 改工具描述 → 必跑 `scripts/eval_agent_routing.py` 对比基线（33/33）
5. 改模型结构 → 必走 Alembic 迁移
6. **跑全量 pytest 前先确认三容器在**（先跑 `python -m scripts.check_env --skip-ai`）；跑完记得 `python -m scripts.seed_kb_preset` 恢复语料——**7.4 测试隔离改造完成后本条"恢复语料"部分作废**
7. **7.3 之后装/升任何依赖必须同步 `requirements.lock`**；7.6 之后每批收口以"push + CI 全绿"为完成标志
8. 启动项目前先跑 `python -m scripts.check_env` 自检（容器闲置会假死）
