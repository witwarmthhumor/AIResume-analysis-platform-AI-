# 后端依赖清单（pip 官方约定文件名）。只写下限，安装时解析最新兼容版本
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30
alembic>=1.13
psycopg[binary]>=3.1          # Postgres 3代驱动（SQLAlchemy 2 推荐）
pydantic-settings>=2.3        # 读 .env 转配置对象
ruff>=0.4                     # 代码格式化/检查

# 阶段1：简历上传与解析
python-multipart>=0.0.9       # FastAPI 接收 multipart 文件上传的必装件
pypdf>=4.0                    # 文本型 PDF 逐页提取文字（纯 Python，无系统依赖）

# 阶段2：AI 分析（OpenAI 兼容协议，DeepSeek/通义换模型只改 .env）
openai>=1.40

# 阶段6：Playground 知识库 RAG（v3.0）
pgvector>=0.3        # SQLAlchemy 的 vector 类型映射；embedding 由 Ollama 提供，无本地模型依赖

# v3.5：混合检索（向量 + BM25 词法，RRF 融合）——中文分词用 jieba，纯 Python 无系统依赖
jieba>=0.42

# v3.4：AI 客服 Agent（LangChain 局部引入，仅 Agent 层使用，现有手写 RAG 不改）
# 锁定 0.3 线：1.x 移除了 AgentExecutor/create_openai_tools_agent（改走 langgraph），
# 本项目 Agent 流式基于 0.3 的 AgentExecutor + BaseCallbackHandler，故上限 <0.4
langchain>=0.3,<0.4
langchain-openai>=0.2,<0.4    # ChatOpenAI（OpenAI 兼容协议，通义/DeepSeek 可用）
# v4.0：Agent v2 编排层（LangGraph 0.2.x，实测与 langchain-core 0.3.86 零冲突；1.x 会强升 core→1.6 破坏 v1，禁升）
langgraph>=0.2.76,<0.3
langgraph-checkpoint-postgres>=2.0.21,<3

# 阶段4：用户认证与异步任务
pwdlib[argon2]>=0.3
PyJWT>=2.8
email-validator>=2.1
celery[redis]>=5.4

# 测试
pytest>=8.0
pytest-cov>=7.0                # 覆盖率统计（7.8 基线用；门槛另批决策）
httpx>=0.27                   # FastAPI 测试客户端的底层依赖
fpdf2>=2.7                    # 测试里现造 PDF 样本（正常/多页/无文字）
