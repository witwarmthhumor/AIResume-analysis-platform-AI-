# AI 协作约定（每次与 AI 协作前必读）

> **本文件是干嘛的**：规定"人 + AI"怎么配合干活——记忆机制（AGENTS.md / PROGRESS.md）、九条交互规则、阶段收尾流程、开场白模板。项目计划见 [PROJECT-PLAN.md](./PROJECT-PLAN.md)。
> **核心思想：AI 会话是无状态的，仓库里的文件就是 AI 的记忆。**

---

## 1. 双文件记忆机制（最重要）

仓库里放两个文件，由 AI 维护、人监督：

| 文件 | 角色 | 更新频率 |
|---|---|---|
| `AGENTS.md` | 项目"户口本"：背景、技术栈、启动命令、代码约定、铁律 | 仅阶段切换或约定变更时 |
| `PROGRESS.md` | 进度日志：已完成 / 进行中 / 踩过的坑 / 下一步 | 每次会话结束前（或每完成一小步） |

**规则：**

- AI 每次会话先读这两个文件再干活；人不复述背景，AI 不凭空猜上下文
- 每次会话结束前，人提醒 AI 更新 `PROGRESS.md`
- 模板见 §5，阶段0建仓库时落成实体文件入库

## 2. 九条交互规则

1. **用仓库文件当记忆**：新会话先读 `AGENTS.md` 和 `PROGRESS.md`，再开始工作
2. **小步走**：一次只做一件事（一个接口 / 一个组件）。做完 → 人在浏览器验证 → git commit → 再进行下一件。禁止"把阶段X做完"式的大指令
3. **先方案后代码**：每阶段开工、以及任何超过 3 个文件的改动，AI 先输出方案（动哪些文件、表/接口长什么样），人确认后再写代码
4. **报错原样贴**：终端报错全文复制给 AI，不概括；同时说明刚做了什么操作。环境问题九成靠原始信息定位
5. **验收权在人**：AI 说"完成"不算完成，人按 PROJECT-PLAN.md 对应阶段的验收 checklist 逐条打勾；不通过就贴现象 + 期望，让 AI 修
6. **禁止超纲**：每次会话明确"只做本阶段的事，不顺手重构、不提前引入下一阶段技术"。AI 主动提出的"顺手优化"一律记到 `PROGRESS.md` 的"下一步"，不当场做
7. **git 是安全网**：小步提交；出问题先 `git diff` 看改了什么，再决定修复或回滚；每阶段封版 tag 是回退锚点
8. **卡壳限时**：一个环境/依赖问题折腾超过 1 小时 → 让 AI 给替代方案（如 Docker 不顺就先 venv + 单独容器跑 Postgres），不死磕单点
9. **命令带讲解**：执行任何终端命令前，先用一句大白话说明这条命令做什么、会改动什么、有没有风险；复合命令（带 && 或管道的）逐段解释；安装、删除、覆盖、git 推送等有实际后果的命令，先讲清影响再执行。人问"这条命令是干嘛的"时，用不讲术语的话重新解释

## 3. 阶段收尾流程（每阶段结束时）

1. 对照 PROJECT-PLAN.md 该阶段的验收 checklist 逐条打勾，全部通过
2. AI 更新 `PROGRESS.md`（阶段完成、踩坑记录、下阶段预告）
3. 合并阶段分支回 `main`
4. 打版本 tag（v0.x），连同 tag 推送 GitHub —— **封版**
5. 进入下一阶段：开新分支 + 开新会话（用 §4 开场白模板）

## 4. 新会话开场白模板（存下来直接用）

```
先读 E:\AIDevelop\AIProject\AGENTS.md 和 PROGRESS.md。
当前进行阶段 N，本次目标：〈一句话，例如"实现简历上传接口"〉。
只做这个范围，开始前先给我方案。
执行命令时，每条先用大白话解释是干什么的再执行。
```

## 5. 两个记忆文件的初始模板

### AGENTS.md 模板

```markdown
# 项目：AI 简历分析 + AI 模拟面试

## 当前状态
- 当前阶段：阶段0（未开工）
- 项目根目录：E:\AIDevelop\AIProject
- 权威计划：PROJECT-PLAN.md（改需求先改它）

## 技术栈
- 后端：FastAPI + Postgres + SQLAlchemy + Alembic（阶段4 加 Celery/Redis）
- 前端：Vue 3 + Vite
- AI：OpenAI 兼容协议（DeepSeek / 通义），Key 只放 .env
- 后台任务：V1 用 FastAPI BackgroundTasks，阶段4 换 Celery

## 启动命令
（阶段0 落地后补全：数据库、后端、前端分别怎么启动）

## 代码约定
- commit message：conventional commits（feat: / fix: / docs:）
- 数据库改动必须走 Alembic 迁移，禁止手动改表
- Python 代码带类型注解，格式化用 ruff

## 铁律
1. API Key 只放 .env，.env 永不入库
2. 只做当前阶段的事，不顺手重构、不提前引入后续技术
3. 改动超过 3 个文件先出方案
4. 测试简历集（PROJECT-PLAN.md §6）是回归基准，改解析/提示词必须重跑
5. 执行任何命令前，先用一句大白话解释它做什么、动什么、有没有风险，解释完再执行
```

### PROGRESS.md 模板

```markdown
# 进度日志

## 当前进行
- 阶段0：未开工

## 已完成
- 项目规划定稿（PROJECT-PLAN.md / AI-COLLABORATION.md）

## 已知问题 / 踩过的坑
- （暂无）

## 下一步
- 阶段0：安装 Docker Desktop + WSL2、Python 3.11+、Node.js 20+
- 准备 GitHub 账号、申请大模型 API Key
```
