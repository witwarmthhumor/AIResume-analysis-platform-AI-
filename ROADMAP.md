# AI简历分析 + AI模拟面试 — 项目路线图

> **本文件是干嘛的**：项目全局路线速查——关键决策、风险对策、阶段拆分与封版计划一页看完。内容与 PROJECT-PLAN.md 有重叠，**权威细节以 PROJECT-PLAN.md 为准**，本文件只作总览。
> 状态：规划已确认（2026-08-31），每阶段封版时回看对照。

## 已确认的关键决策

| 决策点 | 结论 |
|---|---|
| 大模型 | 国产 API（DeepSeek / 通义 / Kimi，统一封装，可切换） |
| 面试形式 | 文字对话先行，远期扩展语音 |
| 用户体系 | 简单注册登录（JWT），表结构第一天就预留 user_id |
| 项目定位 | 学习 + 求职作品集 |
| 版本管理 | 全程 Git；每阶段验收后打 tag 推送 GitHub |

## 风险与对策清单

1. **简历解析是最容易翻车的点**：扫描件PDF提不出文字、双栏/表格排版混乱。对策：V1 只支持文本型PDF，限 5MB / 5 页，失败给明确报错。
2. **AI 输出不可控**：固定 JSON 输出结构（优势/短板/关键词缺口/改进建议/预测面试题）+ 解析校验 + 失败重试。
3. **密钥安全**：API Key 只走环境变量，`.env` 必须进 `.gitignore`——全程用 git+GitHub，这条第一天就要做对。
4. **隐私合规**：简历是敏感个人信息。上传页提示"内容将发送给第三方大模型服务"，不收集多余信息，写进 README。
5. **技术栈分期引入**：V1 只用 FastAPI + Postgres + Vue（后台任务用 BackgroundTasks 过渡），Redis/Celery 阶段4 引入，Docker 从阶段0 逐步用。留好替换点，后续是"换"不是"重写"。
6. **流式输出**：面试回复 10~20 秒/次，V1 就做 SSE 流式。
7. **中文场景优先**：提示词按中文简历+中文面试设计，兼容英文；评分维度提前定义（技术深度、表达结构、项目真实性）。
8. **成本控制**：国产 API 很便宜（一场面试约几分钱），但加每日次数上限防滥用。

## Git / GitHub 管理流程

1. **阶段0 建立**：`git init` → `.gitignore`（排除 `.env`、`node_modules`、`__pycache__`、上传的简历文件）→ GitHub 建仓库（推荐 public）→ 配置推送认证 → 首次推送。
2. **日常节奏**：小步提交，commit message 有意义（如 `feat: 简历解析支持多栏排版`）；每阶段开独立分支（如 `stage-2-ai-analysis`），完成后合并回 `main`。
3. **阶段封版**：验收通过 → 打 tag → 连同 tag 推送 GitHub。
4. Tag 规划：`v0.1` 骨架 / `v0.2` 简历解析 / `v0.3` AI分析 / `v0.4` 模拟面试 / `v0.5` 用户体系 / `v1.0` 部署版。

## 阶段拆分

| 阶段 | 交付物（浏览器里能看到什么） | 预估 | 封版 |
|---|---|---|---|
| 0 环境与骨架 | 一键跑起的空壳：FastAPI 健康检查 + Vue 首页调通后端 + Postgres 容器 + Alembic；git 首推 | 1周 | v0.1 |
| 1 简历上传与解析 | 上传PDF→解析出纯文本→页面展示；类型/大小/页数校验、失败明确报错 | 1~1.5周 | v0.2 |
| 2 AI简历分析 | 结构化报告（岗位匹配/优势/短板/关键词缺口/建议/预测面试题）+ 报告页；API统一封装、JSON校验重试、每日限额 | 1.5~2周 | v0.3 |
| 3 文字模拟面试 | 基于简历的多轮面试（自我介绍→技术题→追问→结束评价报告）；SSE流式、上下文管理 | 2周 | v0.4 |
| 4 用户体系+异步化 | 注册登录、数据按用户隔离、历史记录；迁移到 Celery+Redis + 任务进度 | 1.5~2周 | v0.5 |
| 5 部署与打磨 | `docker-compose up` 一键启动；云服务器可访问；README（架构图/选型/演示账号） | 1~2周 | v1.0 |

总计约 7~9 周（新手节奏，宁慢勿弃）。

## V1 范围（阶段0~3）

**包含**：文本型PDF上传解析（5MB/5页）、AI简历分析报告、文字模拟面试（多轮+追问+结束评价）、SSE流式、每日次数限制、Postgres + BackgroundTasks + 本地 docker-compose。

**不包含**：注册登录（阶段4）、Celery/Redis（阶段4）、语音面试（远期，会话按"消息列表"设计天然兼容）、docx/图片简历与OCR、移动端适配、国际化、支付/配额/管理后台。

## 阶段0 开工前需要准备

1. GitHub 账号（没有先注册一个）
2. 大模型 API Key（届时演示如何申请 DeepSeek 或通义的免费额度）
3. 确认本机已装：Python 3.11+、Node.js、Docker Desktop（阶段0 第一步一起装也行）
